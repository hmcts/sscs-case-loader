server.port: ${PORT:8082}

core_case_data:
  api:
    url: ${CORE_CASE_DATA_API_URL:http://localhost:4452}
  userId: ${CORE_CASE_DATA_USER_ID:userId}
  jurisdictionId: ${CORE_CASE_DATA_JURISDICTION_ID:jurisdictionId}
  caseTypeId: ${CORE_CASE_DATA_CASE_TYPE_ID:caseTypeId}
  eventId: ${CORE_CASE_DATA_EVENT_ID:eventId}

idam:
  url: ${IDAM_URL:http://localhost:4501}
  s2s-auth:
    totp_secret: ${IDAM.S2S-AUTH.TOTP_SECRET:secret}
    microservice: ${IDAM.S2S-AUTH.MICROSERVICE:microservice}
    url: ${IDAM.S2S-AUTH:http://localhost:4502}
  oauth2:
    user:
      email: ${IDAM_SSCS_SYSTEMUPDATE_USER:user}
      password: ${IDAM_SSCS_SYSTEMUPDATE_PASSWORD:password}
    client:
      id: ${IDAM_OAUTH2_CLIENT_ID:clientId}
      secret: ${IDAM_OAUTH2_CLIENT_SECRET:secret}
    redirectUrl: ${IDAM_OAUTH2_REDIRECT_URL:http://localhost}

management.security.enabled: ${MANAGEMENT_SECURITY_ENABLED:true}

sftp:
  keyLocation: ${GAPS2_KEY_LOCATION:./docker/sftp-docker}
  host: ${GAPS2_SFTP_HOST:localhost}
  port: ${GAPS2_SFTP_PORT:2222}
  username: ${GAPS2_SFTP_USER:sftp}
  inputDirectory: ${GAPS2_SFTP_DIR:incoming}

sscs.gaps2.schema.location:
  delta: /schema/SSCS_Extract_Schema_0.8.xsd
  reference: /schema/SSCS_Extract_Schema_Reference_0.2.xsd




